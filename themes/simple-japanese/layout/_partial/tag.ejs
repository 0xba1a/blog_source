<% if (post.tags && post.tags.length){ %>
  <div class="tags">
    <% post.tags.forEach(tag => { %>
      <a class="tag is-white" style="color: darkmagenta; padding-left:0;" href="<%- url_for(tag.path) %>"><%= tag.name %></a>
    <% }) %>
  </div>
<% } %>
